{
  "name": "barber-booking",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack barbering booking platform: Next.js marketing site, Express API, and React appointments dashboard",
  "license": "ISC",
  "author": "Henry Hai Nguyen",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "build:server": "npm run build --prefix server",
    "build:client": "npm run build --prefix client",
    "start:server": "npm start --prefix server",
    "test": "npm test --prefix web && npm test --prefix server",
    "test:e2e": "npm run test:e2e --prefix web",
    "typecheck": "npm run typecheck --prefix web"
  }
}
